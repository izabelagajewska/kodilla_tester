package com.kodilla.abstracts.homework;

public class ShapeProcessor {


    public void process(Shape shape) {
        System.out.println("Formula for a triangle circut is " + "" + shape.getCircut() + "" + "and formula for a triangle area is:" + shape.getSurfaceArea());
    }
}

